debug:
  update_interval: 5s

text_sensor:
  - platform: debug
    # device:
    #  name: "Device Info"
    reset_reason:
      name: "Reset Reason"

sensor:
  - platform: debug
    free:
      name: "Heap Free"
    # fragmentation:
    #   name: "Heap Fragmentation"
    # loop_time:
    #   name: "Loop Time"
    block:
      name: "Heap Max Block"
